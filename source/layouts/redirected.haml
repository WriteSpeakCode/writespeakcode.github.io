<!DOCTYPE html>
%html
  %head
    %link{ rel: "canonical", "href" => "#{current_page.data.redirect_to}" }
    %meta{ "http-equiv" => "content-type", "content" => "text/html; charset=utf-8" }
    %meta{ "http-equiv" => "refresh", "content" => "0;url=#{current_page.data.redirect_to}" }

%body
  %h1Redirecting
  %a{ href: "#{current_page.data.redirect_to}" }
    Click here if you are not redirected.
  %script{ location: "#{current_page.data.redirect_to}" }
